apiVersion: platform.acme.co/v1alpha1
kind: Cluster
metadata:
  name: cluster1
  namespace: default
  annotations:
    render.crossplane.io/composition-path: apis/primitives/cluster/composition.yaml
    render.crossplane.io/function-path: examples/functions.yaml
spec:
  parameters:
    initialNodeCount: 1
    networkRef:
      name: subnetwork1
    subnetworkRef: {}
    project: acme-project
    location: us-central1
